// SalesCoachSheet component structure:
function SalesCoachSheet({ open, onOpenChange, conversationId, onSuggestion }: { open: boolean, onOpenChange: (o: boolean) => void, conversationId: string, onSuggestion: (text: string) => void }) {
  // fetch analysis
  // handle generate analysis
  // handle generate suggestion
}
